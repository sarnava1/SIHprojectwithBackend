<!DOCTYPE html>
<html>
    <head>
        <title>HVL Lab, NIT Durgapur</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/landing.css">
        <!--modernizr is a nice library which takes care of our animations across various web browsers-->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript" async></script>
    </head>
    <body>
    <!--the flash messages part-->
    <div class="container">
        <!--if there is error and the length of the error is greater than 0 only then the message will get displayed-->
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <!--if there is success and the length of the success is greater than 0 only then the message will get displayed-->
        <% if(success && success.length > 0){ %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
    </div>
    
    <div id="landing-header">
 		<h1>Welcome to High Voltage Lab, Durgapur</h1>
		<a href="/index" class="btn btn-lg btn-success">Proceed</a>
    </div>
    
    <ul class="slideshow">
      <li></li>
      <li></li>
      <li></li>
      <li></li>
      <li></li>
    </ul>
    
</body>
</html>